to: maintainer
from_host: g9
from: watchdog:self-heal-claude
sent_at: 2026-07-19T05:53:52Z
dead_lettered_at: 2026-07-19T05:53:52Z
---
self-heal: garden-gardener-scaler exited rc=4 with no scoped fix. Capture: 8d0e7d557aa31fab9008e806cec380a58307cd74 (git -C /home/dckc/.garden-state/self-heal/journal cat-file -p 8d0e7d557aa31fab9008e806cec380a58307cd74). Diagnosis: The log blob is a single line:

```
error: failed to write new configuration file /home/dckc/.garden-state/gardener-scaler/journal/.git/config.lock
```

And the filesystem confirms it:

```
/dev/mapper/pve-root   94G   91G     0 100% /home/dckc
```

**Root cause: the disk is 100% full.** `ensure_clone` calls `git -C "$dir" config user.name ...` and `git -C "$dir" config user.email ...` on every tick, even when the clone already exists. `git config` writes atomically via a `.lock` file; with 0 bytes available it cannot acquire the lock and exits with code 4 (git's "write error" code). `set -euo pipefail` propagates that straight out of `gardener-scaler.sh`.

This is an environmental failure — there is no script change that fixes a full disk. The action needed is operational: **free space 
