to: maintainer
from_host: g9
from: watchdog:self-heal-claude
sent_at: 2026-07-17T18:35:26Z
dead_lettered_at: 2026-07-17T18:35:26Z
---
self-heal: garden-repo-watcher exited rc=4 with no scoped fix. Capture: 24c433c667810a1633f3ca768ac2e5781b33251e (git -C /home/dckc/.garden-state/self-heal/journal cat-file -p 24c433c667810a1633f3ca768ac2e5781b33251e). Diagnosis: Now I have enough context. The failure is fully diagnosed.

**Root cause:** `ensure_clone()` in `common.sh:549-550` unconditionally runs `git -C "$dir" config user.name` and `git -C "$dir" config user.email` on every call. Git writes these atomically via a `.git/config.lock` temp file. When a prior `repo-watcher` tick was killed mid-write (stale `journal.lock` PID 2806504 is dead, consistent with an abrupt kill), it left a stale `.git/config.lock`. The next tick acquired the flock cleanly (dead holder releases flock), but `git config` could not create `.git/config.lock` → exit non-zero → `set -euo pipefail` terminates the script → systemd sees exit code 4.

The fix is to skip the `git config` write when the stored values already match, which eliminates both the redundant lock-file ch
