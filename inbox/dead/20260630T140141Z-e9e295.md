to: maintainer
from_host: g9
from: gardener:deadmail-20260630T135936Z-98e4d6
reply_to: deadmail-20260630T135936Z-98e4d6
sent_at: 2026-06-30T14:01:41Z
dead_lettered_at: 2026-06-30T14:01:41Z
---
**Action needed: deploy on g9**

A gardener (deadmail-20260630T124326Z-9b16ef) investigated the chain of deadmail/POISON jobs from today (~07:03 UTC) and found it was a false alarm — no code bug to fix beyond what is already in main2.

**Root cause:** `comment-watcher/agoric-labs-jesc24` fired a BLIND self-test alert because the deployed `comment-watcher.sh` has a slug-parsing bug: dashed owner slugs like `agoric-labs-jesc24` were parsed as owner=`agoric`, name=`labs-jesc24`, so the self-test hit `agoric/labs-jesc24` (404). The 404 body tripped the same symptom as the 2026-06-24 jq-outage, so it reported BLIND.

**Fix already in main2:** Commit `eb6e8fdd3` (~10h ago) reads `comment-repos/<slug>` for an `owner/name` override before falling back to slug parsing. The journal entry `comment-repos/agoric-labs-jesc24` already contains `agoric-labs/jesc24`. Self-test with the correct repo returns comment id 4792682756 — HEALTHY.

**What needs to happen now:**

Please run `scripts/jobs/deploy-garden.sh` on g9. Note:
- The upgrade monitor (`garden-upgrade-monitor`) is inactive and `state/deploy/` does not exist, so the auto-deploy-on-upgrade signal is not firing — manual deploy is needed.
- A stopgap selftest marker was written to suppress false-alarm cycles for 1 hour; after deploy it is no longer needed.
- A second fix also in main2 needs deploying: commit `8630f22cc` (gardener error transcript blobs now inline first 2048 bytes in the error journal entry, making them accessible off-host).
