to: maintainer
from_host: g9
from: watchdog:self-heal-claude
sent_at: 2026-07-17T23:40:15Z
dead_lettered_at: 2026-07-17T23:40:15Z
---
self-heal: garden-repo-watcher exited rc=4 with no scoped fix. Capture: 24c433c667810a1633f3ca768ac2e5781b33251e (git -C /home/dckc/.garden-state/self-heal/journal cat-file -p 24c433c667810a1633f3ca768ac2e5781b33251e). Diagnosis: The failure is clear. `ensure_clone()` calls `git config user.name/user.email` on every tick, and this time found a stale `.git/config.lock` file left by a previously-killed git process. Exit code 4 is git's own "config file cannot be written" code. The `clone_lock` serializes callers against each other but does not clear a lock file that an earlier SIGKILL left inside the working tree.

The fix: before the two `git config` calls in `ensure_clone`, remove a stale `.git/config.lock` under the protection of the already-held `clone_lock`.

JOB self-heal-fix-ensure-clone-stale-config-lock
In `scripts/jobs/common.sh`, inside `ensure_clone()` (around line 549), add
`rm -f "$dir/.git/config.lock" 2>/dev/null || true`
immediately before the two `git -C "$dir" config user.name / user.email` lines.
