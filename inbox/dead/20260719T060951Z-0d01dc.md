to: maintainer
from_host: g9
from: watchdog:self-heal-claude
sent_at: 2026-07-19T06:09:51Z
dead_lettered_at: 2026-07-19T06:09:51Z
---
self-heal: garden-library-source-drift-scan exited rc=4 with no scoped fix. Capture: b2510fc2a688f4c9e374df3863b4404ca932ad34 (git -C /home/dckc/.garden-state/self-heal/journal cat-file -p b2510fc2a688f4c9e374df3863b4404ca932ad34). Diagnosis: I have the full diagnosis. The root cause is clear:

**Disk 100% full** (`/dev/mapper/pve-root` 94G used, 0 avail). During `ensure_clone`, `git config user.name/email` tries to write `.git/config.lock`, which fails with ENOSPC. Git exits 4 ("config file cannot be written") — a documented git config exit code. With `set -euo pipefail`, the script exits 4. The systemd unit lists `SuccessExitStatus=143 130 75` — exit 4 is not there, so systemd counts it as a hard failure instead of a skip-this-tick.

There IS a concrete code fix: add an early disk-space guard to `library-source-drift-scan.sh` that exits 75 (EX_TEMPFAIL, already in `SuccessExitStatus`) when free space is critically low, giving a clear diagnostic message rather than a cryptic git exit-4 propagating to systemd as a hard fail
