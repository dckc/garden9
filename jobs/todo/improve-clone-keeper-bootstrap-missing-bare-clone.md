Harden `scripts/jobs/clone-keeper.sh` so a missing tracked bare clone is deterministically re-created instead of warned-and-skipped forever. Today `keep_clone()` (lines 74-81) logs `WARN: tracked clone … is missing or not a git repo … skipping` and returns 0 every ~30m tick — which re-forms the exact endo upstream-drift re-ingestion block the keeper's own header (lines 7-13) says it exists to prevent. The current `journalctl` digest shows this WARN firing on `worktrees/endojs-endo.git`, so the endo bare clone is gone and the keeper is permanently idling.

Change: extend the `GARDEN_TRACKED_CLONES` line format from `<dir>|<remote>|<branch>` to an optional fourth field `<dir>|<remote>|<branch>|<clone-url>` (default the endo entry on line 47 to its upstream `https://github.com/endojs/endo.git`). When `keep_clone()` finds the clone missing/not-a-git-repo AND a clone-url is configured, bootstrap it deterministically before continuing: `git clone --bare <url> <abs>` then set the fetch refspec `git -C <abs> config remote.origin.fetch '+refs/heads/*:refs/remotes/origin/*'` (per `WORKTREES.md` lines 43-53, so `origin/*` tracking refs don't freeze). Wrap the clone in `timeout GARDEN_FETCH_TIMEOUT` with the existing backoff/retry discipline; on bootstrap failure (offline/DNS) log once and return 0 (never wedge), matching the existing fetch-failure path. Only fall back to the current WARN-and-skip when no clone-url is configured. Add a hermetic case to `scripts/jobs/test/clone-keeper-test.sh` (alongside the existing one at line 72): a tracked entry whose `<dir>` does not yet exist plus a throwaway upstream bare repo as the URL, asserting the keeper creates the bare clone, sets the refspec, and then fast-forwards on the next tick. This moves "notice the bare clone vanished and re-clone it" off agent discretion and onto the cadence timer, closing the loophole that lets the re-ingestion block re-form whenever the clone is deleted rather than merely stale.
