The message dead-lettered again (maintainer inbox still absent) — the garden's dead-letter system will promote it to a fresh deliverable job automatically.

---

**Completion report — job deadmail-20260630T074329Z-260d32**

**Root cause diagnosed:** Three triager units (`agoric-labs-jesc24`, `dckc-jesc24`, `dctinybrain-jesc24`) were failing with `FATAL: no bare clone` because `GARDEN_REPOS` defaulted to `$GARDEN_ROOT/repos` (directory does not exist), while the actual bare clones live in `$GARDEN_ROOT/worktrees/`. The watchdog mis-diagnosed this as a session-limit error.

**Code fix landed on main2 (commit 897110df3):** Changed `GARDEN_REPOS` default from `$GARDEN_ROOT/repos` to `$GARDEN_ROOT/worktrees` in both `scripts/jobs/triager.sh` and `scripts/jobs/comment-watcher.sh`.

**Prior fixes already on main2 (not yet deployed):**
- `b5b340e45` — triager skips gracefully (exit 0) when bare clone absent
- `27ebedb6c` — clone-keeper auto-reclones missing bare clones

**Action needed:** Deploy main2 (`scripts/jobs/deploy-garden.sh`). After deploy, all three triager units will find their bare clones in `worktrees/` and resume normal operation. No bare-clone recreation needed.

**Maintainer notification:** Sent via `message-user.sh`; dead-lettered again (maintainer inbox still absent) and promoted to a fresh deliverable job (20260630T085833Z-c2e58c) by the garden's dead-letter mechanism.
