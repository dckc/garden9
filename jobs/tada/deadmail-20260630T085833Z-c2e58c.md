The upgrade-ready signal is set at `/home/dckc/.garden-state/deploy/upgrade-ready`. The liaison's Deploy-on-upgrade Monitor watches for this signal and would normally invoke `deploy-garden.sh` automatically — but there's no active liaison Monitor in this session.

---

**Completion report — deadmail-20260630T085833Z-c2e58c**

**What I did:**
- Attempted to forward the dead-lettered deploy request to the maintainer inbox — it dead-lettered again (no active liaison session), creating another deadmail job (20260630T090149Z-0db69b).
- Investigated the actual deploy state directly.

**Current state:**
- `origin/main2` is 6 commits ahead of the deployed garden root (`0ba5bdbb1` → `9b91632c9`). The 6 commits include all three triager/clone-keeper fixes from the original incident.
- The `garden-upgrade-monitor` service already detected this and wrote the "Upgrade ready" signal to `/home/dckc/.garden-state/deploy/upgrade-ready`.
- The signal is ready; the liaison's deploy-on-upgrade Monitor would act on it automatically — but no Monitor is running in this session.

**Action needed from maintainer:**
Run `scripts/jobs/deploy-garden.sh` to deploy main2. After deploy, the triager units (`garden-triager@dctinybrain-jesc24`, `@agoric-labs-jesc24`, `@dckc-jesc24`) will find bare clones in `worktrees/` and resume normal triage. No re-creation of bare clones is needed — they already exist.

**Follow-up:** The new deadmail job (20260630T090149Z-0db69b) will spawn another gardener to re-deliver the same message. Once the deploy happens, no further action on those dead letters is needed.
