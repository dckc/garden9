Inbox empty. Work complete.

---

## Completion report

**Job:** `deadmail-20260629T162915Z-fd50f6` — pick up the intent of a dead-lettered message addressed to `maintainer`.

**Diagnosis.** The dead-lettered message carried **no actionable intent**. Its fingerprint — `from: gardener:--help`, `reply_to: --help`, empty body — uniquely identifies its origin: someone ran `message-user.sh --help`. That script took `--help` as the `<reply-to-doer>` positional, set `GARDEN_REPLY_TO=--help` / `GARDEN_SENDER=gardener:--help`, read an empty stdin body, and sent an empty message to `maintainer`. With no live maintainer inbox on this host it dead-lettered and was promoted into this very job. So rather than carry a message forward, I fixed the **bug that manufactured it**.

**Root cause / fix.** The messaging scripts treated `-h`/`--help` as data instead of a usage query — the exact defect class already fixed in `journal-entry.sh` (stray `kind: --help` entries) and `post-job.sh`. I applied the same established guard — a `usage()` printing the leading comment block plus `case "${1:-}" in -h|--help) usage; exit 0 ;;` placed **before** the positional is consumed — to four scripts:
- `scripts/jobs/message-user.sh` (the entry point that produced this dead-letter)
- `scripts/jobs/inbox-send.sh` (the underlying sender)
- `scripts/jobs/send-msg.sh` (the broadcast-bus sender)
- `scripts/jobs/inbox-read.sh` (read-side; prevents a stray journal clone for a doer named `--help`)

**Verification.** All four pass `bash -n`; `--help`/`-h` now print usage and exit 0 with no send/network/git activity (confirmed for `message-user.sh` and `send-msg.sh`).

**Landed.** Commit `5af76aaca`, pushed to `origin/main2` (clean, first attempt).

**Follow-ups (optional, not blocking):**
- `inbox-list.sh` takes no positional, so it needs no guard; left as-is.
- Consider a `scripts/jobs/test/` case asserting `--help` exits 0 without a push, to lock in this regression across the messaging-script family.
