The `message-user.sh --help` probe was itself treated as a message body and dead-lettered (the `maintainer` inbox is currently gone — the very condition that produced this job). So actively re-messaging `maintainer` right now would just spawn another deadmail cycle. I'll not repeat that.

I have everything needed to dispose of this job. No code change is owed (the fix is already on `origin/main2`), and the deploy that resolves it is the liaison's deliberate action, already armed via the `upgrade-ready` signal.

---

## Completion report

**Job:** `deadmail-20260629T162325Z-32ee13` — pick up the intent of a dead-lettered watchdog self-heal report bound for `maintainer`.

**What the message asked:** A `watchdog:self-heal-claude` on host `g9` reported that `garden-library-link-scan` exits rc=1 every tick with no scoped fix, diagnosing it as a benign steady state needing a *deploy, not code* (message body was truncated mid-sentence at "the fix is already committed on `origin/mai…`").

**What I did — verified the diagnosis against live state (read-only):**
- The fix **is** committed on `origin/main2`: commit `c1b5bbbeb` ("library-link-scan/regenerate-sections-index: skip cleanly when tip has no library tree"). In the worktree, `library-link-scan.sh:118-125` now logs one line and `exit 0` when the tip carries no `library/` tree.
- The **deployed** root (`/home/dckc`) is at `f5c73a78e`, **7 commits behind** `origin/main2` (`3a17c87df`). Its deployed `library-link-scan.sh:115` still has the old fatal `die "no library/ in the synced clone … (tip has no library tree?)"` — exactly the reported failure signature.
- The `garden-upgrade-monitor` **`upgrade-ready` signal is already set** for g9 (deployed `f5c73a78e` → available `3a17c87df`, ahead_by 7, detected 2026-06-29T16:27:23Z).

**Conclusion:** Confirmed deploy lag, not a code gap. g9 is a non-library instance whose journal tip has no `library/` corpus; the already-landed fix turns that benign state into a clean skip. **No fix job / code change is owed**, consistent with the watchdog's own diagnosis.

**What changed:** Nothing committed — no code change was needed or pushed.

**Resolution path / follow-ups:**
- The fix reaches g9 only via a deliberate deploy (`scripts/jobs/deploy-garden.sh`), which the liaison's deploy-on-upgrade Monitor runs on the already-armed `upgrade-ready` signal. I deliberately did **not** run the deploy myself: a gardener job worktree must not drain-and-restart the whole fleet (it would tear down its own runtime), and deploy is the liaison/maintainer's gated action.
- Until that deploy lands, `garden-library-link-scan.service` (and `garden-regenerate-sections-index.service`, same fix) will keep marking Failed each tick on g9 and re-firing this self-heal responder — a benign, self-clearing loop once g9 is deployed.
- **Caveat (minor noise I introduced):** a `message-user.sh --help` usage probe was sent verbatim to the gone `maintainer` inbox and dead-lettered, promoting trivial job `20260629T162915Z-fd50f6` (body: `--help`). It carries no intent and can be discarded. I did not send a further maintainer message, since the inbox is down and it would only re-enter the deadmail cycle; the actionable finding is captured in this report instead.
